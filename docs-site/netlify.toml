# Netlify config for the LitZap docs site (Nextra / Next.js).
# Create this as a SEPARATE Netlify site with base directory = docs-site.
[build]
  base = "docs-site"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
